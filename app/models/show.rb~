class Show < ActiveRecord::Base
  belongs_to :user
  has_many :videos
  attr_accessible :date, :duration, :title, :toppic, :user_attributes
  
    def self.search(search)
    if search
      where("toppic OR title LIKE ?", "%#{search}%")
    else
      scoped
    end
  end
end
