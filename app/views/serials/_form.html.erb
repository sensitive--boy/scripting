<%= form_for @serial do |f| %>

<div class="fields">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class="fields">
  <%= f.label :description %>
  <%= f.text_area :description %>
</div>

<%= f.hidden_field :for_shows %>

<%= f.submit %> | <%= link_to "Zurück ohne Speichern", session[:last_page] %>

<% end %>