<div class="row-fluid">
  <div class="span2">
    <%= render :partial => 'left_menue' %>
  </div>

  <div class="span10">

    <% if current_user.advisor? %>
      <div class='table'>
	<h3>Meine Sendungen</h3>
	<table width="80%">
	  <tr>
	    <th>Titel</th>
	    <th>Thema</th>
	    <th>Sendedatum</th>
	    <th>Status</th>
	  </tr>
	  <% @shows.each do |show| %>
	    <tr>
	      <td><%= link_to show.title, edit_show_path(show) %></td>
	      <td><%= show.toppic %></td>
	      <td><%= l show.date, :format => :g_date %></td>
	      <td></td>
	    </tr>
	  <% end %>
	</table>
      </div>
<hr>
      <div class='table'>
	<h3>Beiträge, die ich betreue</h3>
	<table width="80%">
	  <tr>
	    <th>Titel</th>
	    <th>verantwortlich</th>
	    <th>Sendung</th>
	    <th>Status</th>
	  </tr>
	  <% @supervised.each do |video| %>
	    <% if video %>
	      <tr>
		<td><%= link_to video.title, edit_video_path(video) %></td>
		<td><%= video.user.person.name %></td>
		<td><%= video.show.title if video.show %></td>
		<td><%= video.state %></td>
	      </tr>
	    <% end %>
	  <% end %>
	</table>
      </div>
     <% end %>
<hr>
    <div class='table'>
      <h3>Eigene Beiträge</h3>
      <table width="80%">
	<tr>
	  <th>Titel</th>
	  <th>verantwortlich</th>
	  <th>Sendung</th>
	  <th>Status</th>
	</tr>
	<% @allmyvideos.each do |video| %>
	  <% if video %>
	    <tr>
	      <td><%= link_to video.title, edit_video_path(video) %></td>
	      <td><%= video.user.person.name %></td>
	      <td><%= video.show.title if video.show %></td>
	      <td><%= video.state %></td>
	    </tr>
	  <% end %>
	<% end %>
      </table>
    </div>
  </div>
</div>