<div class="row-fluid">
  <div class="span2">
  </div>
  <div class="span10">
    <h1>Take <%= @take.position %> in Szene: <%= @take.szene.title %></h1>

    Duration: <%= @take.duration %>
<hr>
    View description:<br>
    <%= @take.view_desc %>
<hr>
    Audio description:<br>
    <%= @take.audio_desc %>
<hr>
    Ort:<br>
    <% if @take.address %>
    <%= @take.address.venue %>, <%= @take.address.addition %>, <%= @take.address.address %>, <%= @take.address.city %><br>
    <%= @take.address.country %>
    <% end %>
<hr>
    Bilder:
    <% @take.pictures.each do |pic| %>
      <div class="picture">
	<%= image_tag pic.image_url(:thumb) if pic.image? %><br>
	<%= pic.description %>
      </div>
    <% end %>
<hr>
    Note:<br>
    <%= @take.note %>
<hr>
    <%= link_to "Bearbeiten", edit_take_path(@take) %> | <%= link_to "Zurück zum Script", script_video_path(:id => @take.szene.sequence.video.id) %>
  </div>
</div>