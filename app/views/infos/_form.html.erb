<%= form_for(@info) do |f| %>
  <% if @info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@info.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
      <% @info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :function %><br />
    <%= f.text_field :function %>
  </div>
  <div class="field">
    <%= f.label :addition %><br />
    <%= f.text_field :addition %>
  </div>
    <%= f.hidden_field :szene_id %>
  <div class="actions">
    <%= f.submit %> | <%= link_to "Abbrechen und zurück zur Szene", edit_szene_path(@info.szene.id) %>
  </div>
<% end %>
